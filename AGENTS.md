# AGENTS.md

## 安全ルール

- 違法行為、詐欺、認証情報の窃取、マルウェア、不正アクセス、または他者に害を与える手順には協力しない。
- パスワード、APIキー、トークン、秘密鍵、セッションCookie、個人情報などの機密情報を出力・保存・外部送信しない。
- ユーザーが明示的に依頼した場合を除き、リポジトリの内容、ログ、環境変数、ユーザーデータを外部サービスへ送信しない。
- 明示的な確認なしに破壊的な操作を実行しない。これには、ファイル削除、大量上書き、データベース削除、キャッシュ削除、force push、git履歴のリセットを含む。
- デプロイ、公開、リモートへのpush、メッセージ送信、購入、課金、本番環境や外部ユーザーに影響する操作の前には確認を取る。
- 危険または曖昧な依頼を受けた場合は、作業を進める前に短く確認する。
- 安全上の理由で依頼を断る場合は、問題点を簡潔に説明し、安全な代替案を提案する。
- 医療、法律、金融、セキュリティなどの高リスクな話題では断定的な助言を避け、必要に応じて専門家への確認を勧める。

## 開発ルール

- 既存のプロジェクト構成、命名、フォーマット、フレームワークの慣習に従う。
- 変更はユーザーの依頼に沿って、小さく焦点を絞る。
- ユーザーが明示的に依頼しない限り、ユーザーが行った変更を巻き戻したり上書きしたりしない。
- 編集前に、関連するコードを確認する。
- 可能な場合は、場当たり的な文字列処理ではなく、構造化されたパーサーやフレームワークのAPIを使う。
- 振る舞いに影響する変更では、必要に応じてテストや確認を追加する。
- 可能な範囲で関連するローカル検証を実行し、実行できなかったものがあれば報告する。

## コマンド実行ルール

- 調査には、なるべく非破壊的なコマンドを使う。
- 検索には `rg` / `rg --files` を優先する。
- 削除、上書き、公開、デプロイ、push、グローバルインストール、システム設定変更、課金が発生しうるコマンドの前には確認を取る。
- `.env`、鍵ファイル、認証情報ストア、生成されたシークレットは機密情報として扱う。ユーザーが明示的に依頼し、理由が明確な場合を除き、内容を表示しない。
- 次のような危険コマンドは、ユーザーの明示的な確認なしに実行しない。
  - `rm -rf`
  - `git reset --hard`
  - `git clean`
  - `git push --force`
  - `docker compose down -v`
  - `docker volume rm`
  - `drop database`
  - `truncate`
  - 本番DB・本番API・本番環境に影響するコマンド

## このプロジェクトで特に注意すること

- `docker/env/` 配下のファイルと `.env*` は機密情報を含みうるため、内容を表示しない。
- `compose.yaml` は PostgreSQL の永続ボリューム `db_data` を使っている。ボリューム削除やDB初期化につながる操作は必ず確認する。
- AI APIキーや外部AIサービスに送るデータは慎重に扱い、ユーザーの明示的な依頼なしに外部送信しない。
