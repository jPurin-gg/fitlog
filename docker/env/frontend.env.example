# 公開バックエンドAPIのURLです。
# 空欄にすると、ブラウザは同一オリジンの /api を呼び、Next.js が Docker 内部のバックエンドへ中継します。
# 注意: NEXT_PUBLIC_API_URL はフロントエンドのビルド時に埋め込まれるため、
# 本番ビルドではルートの `.env.prod` にも同じ値を設定してください。
NEXT_PUBLIC_API_URL=

# Next.js が /api リクエストを中継するDocker内部のバックエンドURLです。
BACKEND_INTERNAL_URL=http://backend:8080
